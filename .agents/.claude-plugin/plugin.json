{
  "name": "mailbox",
  "description": "Multi-mailbox IMAP/SMTP control plane exposed as a REST API and MCP server on a single port — read, search, send, mark-seen, and delete mail across multiple inboxes.",
  "author": {
    "name": "psyb0t",
    "url": "https://github.com/psyb0t"
  },
  "homepage": "https://github.com/psyb0t/docker-mailbox",
  "license": "WTFPL",
  "keywords": [
    "email",
    "imap",
    "smtp",
    "mailbox",
    "mcp",
    "rest-api",
    "self-hosted"
  ],
  "userConfig": {
    "mailbox_url": {
      "type": "string",
      "title": "mailboxd URL",
      "description": "Base URL of your running mailboxd instance, e.g. http://localhost:8000 (sets MAILBOX_URL).",
      "required": true
    },
    "mailbox_token": {
      "type": "string",
      "title": "mailboxd bearer token",
      "description": "Bearer token, only needed if the server was started with auth.tokens configured (sets MAILBOX_TOKEN).",
      "sensitive": true
    }
  }
}
